# 仓库管理系统 v3.99

从一份损坏的《仓库进出管理.xlsm》改造而来。原表 `#REF!` 满屏、公式不覆盖新行，
现在改成**单据流水驱动**——只录进出，库存自动算。

面向模切行业：卷料按「卷」和「平米」双口径记账，长 × 宽 自动折算，
零头自动写进备注；采购单与库存**完全解耦**，只靠批次号联系。

---

## 一、快速开始

### 手机（Termux）

```bash
pkg update && pkg install python
cd ~/wh/warehouse
chmod +x run.sh
./run.sh
```

首次运行自动装 Flask、建 `warehouse.db`。看到提示后浏览器打开
**http://127.0.0.1:8080**

```bash
PORT=9000 ./run.sh                      # 换端口
WAREHOUSE_DB=/sdcard/wh.db ./run.sh     # 数据库放别处
```

### 电脑（Windows）

双击 `打包成exe.bat`，产物 `dist\仓库管理系统.exe`，双击自动开浏览器。
详见 `打包exe说明.md`。

> PyInstaller 不支持交叉编译，**exe 只能在 Windows 上打**。

---

## 二、六大模块（v3.37 起模块化）

整个系统拆成六个互不纠缠的模块，**谁先谁后、怎么拼在一起由 `wh/dispatch.py` 决定**。
单个模块可以独立看、独立改，不用在几千行里翻。

| 模块 | 目录 | 行数 | 管什么 |
|---|---|---|---|
| **制表** | `wh/sheet/` | 8139 | **电子表格**：内核 / 求值 / IO / 动作 / Web 五层 + 批次号 + 参考模板（内核函数库 10 个子模块、求值层 8 个、动作层 6 个、Web 层 8 个） |
| **表格** | `wh/table/` | 1654 | 通用录入表：列定义、模板、导入预览、录入表单 |
| **采购** | `wh/po/` | 2014 | 采购单、模板、批次、到货、付款（拆 6 个逻辑文件 + web/ 6 个路由文件） |
| **出入库** | `wh/txn/` | 1054 | 入库、出库、流水、Excel 导入（拆 3 个文件：录入 / 导入 / 列表删除） |
| **统计** | `wh/stat/` | 1127 | 首页、月报、**统计中心八张表**（拆 2 个文件：月报 / 统计中心） |
| **盘点** | `wh/count/` | 455 | 盘点单、实盘、差异、调整入账 |
| **库存** | `wh/inv/` | 406 | 物料档案、库存视图、批量操作 |
| **UI** | `wh/ui/` | 443 | 配色、骨架、组件 |
| **核心** | `wh/core/` | 2536 | 数据库（拆 7 个子模块）、路由、错误处理、工具、通用报表导出 |
| **调度** | `wh/dispatch.py` | 143 | 装配以上所有模块 |

业务模块**不互相 import**，需要制表能力就调表格模块的门面 `tbl`：

```python
from ..table import tbl
tbl.fingerprint(headers)          # 列名指纹（判断两张表是不是同一类）
tbl.evaluate('qty*price', vals)   # 公式求值
tbl.preview(path)                 # Excel 导入预览
tbl.xlsx_response(headers, rows, '库存.xlsx')   # 导出
```

---

## 三、表格模块：类 Excel 制表台

自研制表台按主流开源表格项目的能力清单自建，**零业务耦合、可独立运行**，
依赖只有 `openpyxl`。没用 Handsontable（商用要钱）、Luckysheet（2024-11 已停更）。

**双引擎**（v3.77 起统一为单一入口，默认新引擎，可一键切换）：

| 引擎 | 授权 | 定位 |
|---|---|---|
| **Univer**（默认） | Apache-2.0 | 工业级渲染，Canvas，500+ 函数、条件格式、筛选、图表、透视表 |
| **自研制表台** | — | 离线兜底，175 个公式，Univer 资源缺失时自动接管 |

图表用 **ECharts**（Apache-2.0）、透视表用 **VTable**（MIT）——这两样 Univer 官方放在
Pro 商业版，用同等授权的开源件补齐，不碰商业授权。

计算层另有 **IronCalc**（Apache-2.0 / MIT 双许可）作交叉验算后端，`wh/table/ironcalc_backend.py`，
不装也不影响主程序运行。

> ⚠️ **Univer 是 Open Core 模式**：核心仓库 Apache-2.0，但部署包分 Free / Pro。
> Free 未授权版有限制：**导入文件 ≤ 1MB、导出 ≤ 10000 个单元格、打印 ≤ 3 页**、
> 每文档 1 个透视表、图表仅线/柱/饼/条 4 类。深度定制需向官方取得授权。

### 电子表格（访问 `/sheet`）

| 能力 | 说明 |
|---|---|
| **函数** | **175 个**：数学、统计、文本、日期、查找、逻辑、财务、信息、数据库 |
| **公式** | A1 引用、区域、跨表 `'第一表'!A1`、命名区域、相对/绝对/混合引用 |
| **样式** | 粗体、斜体、下划线、删除线、字号、字体、字色、背景、对齐、垂直对齐、自动换行、缩进、文字旋转 |
| **边框** | 细致边框：上下左右分别设线型（thin/medium/thick/dashed/dotted/double）和颜色 |
| **编辑** | 撤销/重做、剪切/复制/粘贴、填充柄、序列填充、查找替换、格式刷 |
| **数据** | 排序、筛选、自动筛选、分类汇总（插入公式小计行）、删除重复项、数据有效性 |
| **结构** | 多行表头合并、冻结窗格、隐藏行列、单元格批注、条件格式、图表、透视表 |
| **快捷键** | Ctrl+B/I/U 加粗斜体下划线，Ctrl+; 填当天日期，Enter 下一行，Tab 下一格 |
| **导入导出** | xlsx / csv 双向，样式、公式、合并、列宽行高都保留 |

### 列与模板

- **模板 = 一套列定义**。不同表格列不同，就建不同模板，互不干扰
- **列名指纹**：表头去空格、去括号、排序后拼接。顺序不同、括号写法不同
  都算**同一类**；少一列或多一列算不同类
- **导入自动识别**：上传后算指纹，命中就自动选中对应模板
- **按表头一键建模板**：粘贴一行列名，直接生成
- **自定义列**：文本 / 数字 / 日期 / 单选 / **公式**（公式列后端重算，
  手改表单骗不过）

---

## 四、采购与库存：两套账，只靠批次联系

这是 v3.30 之后的核心设计。

### 关系图

```
采购单 ──(建单生成批次号)──▶ 明细行
                                │
入库页填批次号 ─────────────────┘
        │
        ├──▶ 一份进库存（txns）
        └──▶ 一份进采购流水（用于结单）
```

**删掉入库单，采购的到货数不变。两套账没有外键绑定。**

### 到货按入库录入算（v3.35）

采购页**不再手工登记到货**。在入库页录一笔、填上批次号，保存后自动一分为二：

| 动作 | 结果 |
|---|---|
| 入库 25 平米（换算率 5） | 采购记到货 **5 卷**，状态 → 部分到货 |
| 再入库 25 | 凑够 10 卷，状态 → **已完成** |
| 再入库 50 | 已交齐，这次不计入，并提示 |
| 删掉入库单 | 采购到货数**纹丝不动** |

### 采购模板（v3.36）

和库存模板**是两套独立体系**，互不影响：

- 建模板：空白 / 复制 / **按表头建**
- 配列：改名、排序、开关、配单位、改别名
- 自定义列：5 种类型，导出时自动进表头
- 批次是模板的一个可开可关的列：开了自己填，关了自动生成

---

## 四之二、盘点（v3.40 新增）

标准账实核对流程，字段对齐通用盘点单模板：

```
建盘点单（按账面数生成空白表）
   → 打印出来现场盲盘，填「实盘数」
   → 自动算「差异 = 实盘 − 账面」「差异率」
   → 填差异原因、处理意见
   → 点「调整入账」，按差异生成盘盈入库 / 盘亏出库
```

**盘点单本身不动库存**，只有最后「调整」那一步才生成单据。好处：

- 盘点期间可以继续收货发货，不受影响
- 差异原因、处理意见、谁盘的谁监盘，全都留得下痕迹
- 调错了可以「撤销调整」，删掉本次生成的单据，库存回到盘点前

| 操作 | 库存 | 说明 |
|---|---|---|
| 建单 / 填实盘 / 保存 | **不动** | 账面数是建单那一刻的快照，之后出入库不会改它 |
| 调整入账 | 生成单据 | 盘盈→入库单，盘亏→出库单，备注写明原因和单号 |
| 撤销调整 | 删单据 | 按记录的单据 id 精确删除 |
| 删盘点单 | 不动 | 已调整的单子要先撤销，避免留下无主单据 |

导出格式：盘点单号 / 日期 / 范围 / 物料编码 / 名称 / 规格 / 单位 /
**账面数量 / 实盘数量 / 差异数量 / 差异率** / 单价 / 差异金额 /
差异原因 / 处理意见 / 盘点人 / 监盘人 —— 差异行自动标红。

---

## 四之三、统计中心（v3.40 重做）

访问 `/stat`，八张表，覆盖通用仓储统计报表的常见口径：

| 表 | 内容 |
|---|---|
| 收发存汇总 | 期初 / 本期入库 / 本期出库 / 期末 / 单位成本 / 期末金额 |
| 周转分析 | 平均库存 / 本期出库 / 周转率(次) / 周转天数 |
| 呆滞库存 | 期末库存 / 库龄(天) / 库龄区间 / 金额 / 处理建议 |
| ABC 分类 | 期末金额 / 累计占比 / A·B·C / 管理重点 |
| 安全库存预警 | 当前 / 安全库存 / 差额 / 状态 / 建议措施 |
| 进出趋势 | 近 24 个月 进 / 出 / 净额 / 金额 / 单据数 |
| 维度统计 | 按类型、按供应商 汇总进出数量与金额 |
| 盘点差异 | 每次盘点的盘盈盘亏种类、数量、金额 |

**口径写死在这里，避免各处各算一套：**

```
期初 = 档案期初 + 期间开始前的进 − 期间开始前的出
期末 = 期初 + 本期入库 − 本期出库
平均库存 = (期初 + 期末) / 2
周转率 = 本期出库 / 平均库存        周转天数 = 期间天数 / 周转率
库龄 = 今天 − 最后一次出入库日期    呆滞 = 库龄 ≥ 90 天且还有库存
金额 = 数量 × 最近一次填过的单价（没填过单价的按 0 计，不瞎估）
```

每张表都能导出 Excel / CSV（带防公式注入处理）。

---

## 五、模切行业的卷料口径

### 长 × 宽 → 平米 → 卷数

```
一卷平米 = 长 × 宽
卷数     = 总数量 ÷ 一卷平米（向下取整）
余料     = 零头，自动写进备注
```

例：长 100 米 × 宽 0.05 米（一卷 5 平米）

| 你填的数量 | 平米 | 卷料 | 备注 |
|---|---|---|---|
| 250 | 250 | **50** | — |
| 253 | 253 | **50** | 余料 3 平米 |

### 双向自动换算

- 先填**平米** → 自动算卷数（取整，有零头写备注）
- 先填**卷数** → 自动算平米
- 数量框旁有「数量按 平米 / 卷」下拉，两种填法都支持

### 单位：每列自己配，不再有独立单位列

在「⚙ 改表头」里给每列填单位，表头就显示成 `长（米）`、`宽（米）`、
`平米（平米）`、`卷料（卷）`。**留空就是不带单位**。

---

## 五之二、导航结构（v3.46 重做）

底部一行 7 个入口，不再堆成两三行：

```
📊首页  📥入库  📤出库  📗制表  🛒采购  🗂物料  📈统计
```

进入模块后才在导航上方浮出**二级菜单**，用完即收：

| 一级 | 二级 |
|---|---|
| 制表 | 制表台、**模板** |
| 物料 | 档案、库存、**盘点** |
| 统计 | 统计、**流水**、月报 |
| 采购 | 台账、汇总、模板、供应商 |
| 入库 / 出库 | 录入、单据 |

三个"并入"：**模板→制表**、**流水→统计**、**盘点→物料**。
路由一个没动，只是入口换了位置，老书签照样能开。

导航条本身：一行高、半透明磨砂、只在当前模块高亮，不抢内容空间。

---

## 六、页面一览

| 页面 | 路径 | 用途 |
|---|---|---|
| 首页 | `/` | 今日/本月进出、库存预警、最近单据 |
| 入库 | `/in` | 只做入库，卡片默认 1 张，可加行或整片粘贴 |
| 出库 | `/out` | 只做出库，超库存自动拦截 |
| 录单 | `/txn` | 通用，一行里选进或出 |
| 流水 | `/txns` | 全部单据，单价按采购实价映射 |
| 库存 | `/stock` | 实时库存 = 期初 + 入 − 出 |
| 物料 | `/materials` | 档案增删改查、批量改、导入 |
| 月报 | `/report` | 物料 × 每日进出 × 月末结存 + 全表总汇分析 |
| 采购台 | `/purchase` | 采购单列表 |
| 采购模板 | `/po/tpls` | 建模板、配列 |
| 库存模板 | `/tpls` | 建模板、配列 |
| 改表头 | `/columns` | 显示名、列序、开关、单位、导入别名 |
| 制表台 | `/sheet` | 类 Excel 电子表格 |
| 盘点 | `/stk` | 盘点单列表、建单、填实盘、调整入账、导出/打印 |
| 统计中心 | `/stat` | 收发存、周转、呆滞、ABC、预警、趋势、维度、盘点差异 |
| 自检 | `/sys` | 数据体检、备份、清理 |

---

## 七、数据怎么保住的

| 机制 | 作用 |
|---|---|
| 外键约束 | 删物料不会留下无主单据 |
| WAL 模式 | 写入时读不卡，断电不易损坏 |
| 事务 | 批量删除/导入/列映射，要么全成功要么全回滚 |
| 一次性令牌 | 挡住网络卡顿时连点两下造成的重复记账 |
| 写前校验 | 数量非数字、NaN、负数一律挡下 |
| 在线备份 | SQLite 官方 backup API，拷出来的库一定一致 |
| 启动自检 | 数据库损坏时给出明确指引，不清空数据 |

**换手机 / 重装**：把 `warehouse.db` 复制走即可，全部数据就这一个文件。

---

## 八、备份与迁移

```bash
cp warehouse.db warehouse_$(date +%F).db     # 备份
cp warehouse.db ~/storage/downloads/         # 导出到下载目录
```

出问题把 `.bak` 改名回 `warehouse.db` 还原。

---

## 九、文件说明

```
run.py               启动入口（起服务、开窗口/浏览器、兜住崩溃）
wh/dispatch.py       调度文件：装配六大模块
wh/importer/         导入（3 文件，依赖单向：base <- wide <- txn）
  ├ base.py          读取原语、物料档案表头映射、parse_file
  ├ wide.py          原 Excel「宽表」：物料一行 × 每日进/出两列
  └ txn.py           流水导入：表头别名、按列映射、整文件解析
wh/desktop/          桌面外壳（2 文件）
  ├ shell.py         单实例锁、端口、日志、原生窗口（webview）
  └ qt.py            Qt WebEngine 窗口（可选后端，未安装自动跳过）
wh/core/             核心：路由、错误处理、工具、通用报表导出
  ├ paths.py         路径与环境：数据目录、只读资源目录、单位字典
  ├ dbconn.py        连接与查询原语：复用连接、写锁排队、q / run / runmany
  ├ schema.py        表结构：视图与建表 SQL、字段迁移、初始化
  ├ query.py         常用业务查询：物料流水、库存行、批量改删
  ├ tpl.py           模板体系：列映射、列名指纹、自定义列、库存模板
  ├ potpl.py         采购模板
  ├ maintain.py      运维：完整性检查、孤儿修复、备份、统计概览
  ├ db.py            门面：把上面七个聚合成一个 db 命名空间（调用方无感）
  └ export.py        通用报表导出（零依赖，被统计模块共用）
wh/sheet/            制表模块（电子表格，分层）
  ├ kernel/          词法 · 语法 · 地址 · 函数表 · 样式   ← 零业务耦合
  │   ├ convert.py   类型转换与比较原语（to_num / to_serial / _same）
  │   ├ num.py       数学与三角（SUM / ROUND / SIN…）
  │   ├ stat.py      统计（AVERAGE / STDEV / SLOPE…）
  │   ├ logic.py     逻辑（IF / AND / OR / XOR）
  │   ├ text.py      文本（LEFT / MID / SUBSTITUTE / TEXT…）
  │   ├ dt.py        日期与时间（DATE / DATEDIF / NETWORKDAYS…）
  │   ├ lookup.py    查找与引用（VLOOKUP / INDEX / MATCH / OFFSET…）
  │   ├ info.py      信息（ISERROR / ISREF / TYPE…）
  │   ├ finance.py   财务（PMT / IRR / SLN / DB…）
  │   ├ registry.py  函数注册表：包装器 + 装配 FUNCS 表 + 聚合导出
  │   └ funcs.py     门面：外部 `funcs.to_num` / `F.FUNCS` 用法不变
  ├ engine/          数据模型 · 求值器 · IronCalc 第二计算层
  ├ io/              xlsx · csv 读写
  ├ ops/             分组 · 撤销 · 进阶动作
  ├ web/             路由 · Univer 前端集成（4 文件）
  │   ├ common.py    蓝图、存取辅助、引擎偏好（其余三个共享）
  │   ├ pages.py     页面路由：列表 / 新建 / 打开 / 重命名 / 删除
  │   ├ io_rt.py     导入导出：xlsx 导入、导出 xlsx / csv
  │   ├ api.py       /sheet/api/<bid>/<act> 分发与全部 a_* 动作
  │   └ univer.py    Univer 引擎：资源检测、快照互转、/univer 路由
  ├ batch.py         批次号（A 列自动编号）
  └ ref.py           参考模板（把在用模板摊成电子表格）
wh/table/            通用录入表：列定义、模板、导入预览、录入表单
wh/inv/              库存模块：物料档案、库存视图
wh/txn/              出入库模块（3 文件）
  ├ form.py          单据录入：入库 / 出库 / 流水三页共用
  ├ importer.py      流水 Excel / WPS 导入 + 导入模板
  └ browse.py        流水列表、批量操作、删除（含撤采购到货）
wh/po/               采购模块（6 个逻辑文件 + web/ 9 个路由文件，共 2014 行）
  ├ amount.py        金额口径、物料指纹、价格映射   ← 无内部依赖，本包最底层
  ├ head.py          单头金额、已付、欠款
  ├ summary.py       汇总快照（历史对账）
  ├ status.py        状态推导（由到货事实推，不手工维护）、单号
  ├ receive.py       到货入库（采购↔库存的唯一连接点）
  ├ query.py         首页看板、供应商列表
  └ web/             路由：common / home / new / detail / settle / order
                     / tpl / supplier / export（order.py 拆为 5 个）
wh/stat/             统计模块（2 文件）
  ├ report.py        首页洞察、月报表、库存与流水导出
  └ center.py        统计中心八张表与导出（口径写死在这里）
wh/count/            盘点模块：盘点单、实盘、差异、调整入账
wh/ui/               UI 模块：配色、骨架、组件
templates/           页面模板（移动端优先）
warehouse.db         运行后自动生成的数据文件
```

---

## 十、版本要点

| 版本 | 改动 |
|---|---|
| v3.20 | 移除件数/每件 |
| v3.21 | 取消单位列，改为每列自带单位；卷料记卷数 |
| v3.28 | 平米 ↔ 卷料双向自动换算 |
| v3.30 | 采购与库存完全独立，只靠月报联系 |
| v3.33 | 卡片默认 1 张；付款金额默认按应付带出 |
| v3.34 | 采购明细指纹；流水页按采购实价映射 |
| v3.35 | 批次号桥接，到货按入库录入算 |
| v3.36 | 采购模板完整功能 |
| v3.37 | 六大模块拆分 + 调度文件统合 |
| v3.38 | 表格模块对齐 Excel/WPS；修复缩进导出崩溃 |
| v3.39 | 修复领料出库虚增采购到货（欠货显示已完成 → 多付货款） |
| v3.40 | 新增盘点（盲盘→差异→调整）；统计中心重做为八张表 |
| v3.41 | 修复 Python 3.12 启动崩溃（`ast.Num` 被移除） |
| v3.42 | 批次常驻卡片；建单自动生成批次号 |
| v3.43 | 架构归位：录入卡片 = 表格表头映射 |
| v3.44 | 批次号成为表格内建能力（A 列自动编号） |
| v3.45 | 工具栏 64 键压到 11 个；列头菜单；行分组折叠 |
| v3.46 | 导航重做：7 个一级入口 + 二级菜单 |
| v3.47 | 一级「物料」改名库存；修复单价来源标签恒显示「手填」 |
| v3.48 | 二级菜单首项改名档案 |
| v3.49 | 接入 Univer 引擎；exe 自动开独立窗口 |
| v3.53 | 修复导入退料虚增到货；导入预览加「计入采购」开关 |
| v3.59 | 修复筛选/数据验证插件静默失效；补挂十字准星、水印 |
| v3.60 | 接入 ECharts 补齐图表（柱/线/饼/面积/散点） |
| v3.61 | 导出改用 xlsxwriter（装不上自动回退）；统计八表配图 |
| v3.62 | 接入 VTable 补透视表（该项 Univer 官方放 Pro 版） |
| v3.63 | 程序图标、单实例、Inno Setup 安装程序、只读目录避让 |
| v3.64 | 独立窗口改用 Qt WebEngine（自带 Chromium，不依赖 WebView2） |
| v3.65 | 压测优化（5 万单据）14 页面全亚秒；修复含税价当不含税 |
| v3.66 | 修复采购汇总「已付」显示 0（快照缺失时 LEFT JOIN 全 NULL） |
| v3.67 | 采购明细加「单价口径」列；到货数漂移检测与一键修复 |
| v3.68 | 修复流水导出静默截断到 2000 条（改 20 万上限） |
| v3.69 | 修复盘点汇总虚假盘亏、建新单验证 |
| v3.70 | 修复 MATCH 精确匹配失效（INDEX+MATCH 完全不可用） |
| v3.71 | 公式链剥离为可插拔层（2748 行，零业务耦合） |
| v3.72 | 新增 IronCalc 计算层作交叉验算，揪出 12 处算错 |
| v3.73 | 修复三角函数角度单位错误（SIN/COS/TAN 等 6 个） |
| v3.74 | 修复 ROW/COLUMN（拿到值而非坐标）、WEEKDAY 周序偏移 |
| v3.75 | 修复错误捕获类（ISERROR/ISNA/ISREF 等错误被直接传播） |
| v3.76 | 修复 SUMIFS/OFFSET/LOOKUP/TIMEVALUE 等 9 个函数 |
| v3.77 | 表格统一为单一入口（默认新引擎，可切换，旧台作退路） |
| v3.78 | CSV 与 Excel 口径对齐（支持搜索/筛选/排序）；月份改为可选 |
| v3.79 | CELL 改惰性求值，能拿到引用坐标 |
| v3.80 | 修复 HOUR/MINUTE/SECOND 不认时间文本 |
| v3.81 | 盘点全部差异被拦截时不再误标「已调整」 |
| v3.82 | 统计表补规格列（周转/ABC/预警）；修复预警表多余样式列 |
| v3.83 | 修复维度统计合计翻倍；趋势表按区间过滤；洞察榜补料号规格 |
| v3.84 | 结构优化①：抽出制表内核 `wh/sheet/kernel/`（词法·语法·地址·函数表·样式） |
| v3.85 | 结构优化②：抽出求值层 `wh/sheet/engine/`（数据模型·求值器·IronCalc） |
| v3.86 | 结构优化③：抽出 IO 层 `wh/sheet/io/`；报表导出下沉为 `wh/core/export.py` |
| v3.87 | 结构优化④：抽出动作层 `wh/sheet/ops/`；撤销栈下沉，消除对路由层的反向依赖 |
| v3.88 | 结构优化⑤：抽出 Web 层 `wh/sheet/web/`（路由·Univer）；批次号与参考模板归入制表模块 |
| v3.95 | **Web 与引擎去臃肿**：`sheet/web/api.py`(590) 拆为 edit / view / fmt / book 四个动作模块 + 90 行门面（22 个 a_* 按主题归位，`_j` 由 edit 供 book 复用）；`po/web/order.py`(554) 拆为 common / home / new / detail / settle 五个模块（13 条路由，依赖单向）；`engine/core.py`(547) 拆为 cell / sheet / book 三个模块 + 19 行门面。**顺带修复**：`po/web` 三处跨模块名未导入、`core/util.py` 缺模块级 `QTY_MAX`（盘点批量录入 `hi=QTY_MAX` 会 NameError）、`core/paths.py` 的 `unit_choices` 用了未定义的 `q`（异常被 try 吞掉，单位候选丢失历史项）；清理 9 行拆分残留的重复 import；`warehouse.spec` 清单重生成（133 条、0 缺失） |
| v3.94 | **求值层与动作层去臃肿**：`engine/eval.py`(830) 拆为 ctx（求值原语）/ lazy_logic / lazy_cond / lazy_agg / lazy_ref（四类惰性函数）/ lazy（登记表）/ eval（AstEvaluator）七个模块，依赖严格单向；`ops/extra.py`(652) 按主题拆为 clip / fill / valid / analyze / fmt 五个模块 + extra 登记表，`_push_snap`、`_undo_hooks` 归入 undo 撤销栈。**顺带修复**：词法层不认 `$`，导致绝对引用 `$A$1` 全部报 `#NAME?`（地址层 parse_ref 一直支持，只是词法没放行） |
| v3.93 | **函数库去臃肿**：`kernel/funcs.py`(1791) 按类别拆为 convert / num / stat / logic / text / dt / lookup / info / finance 九个模块 + registry（注册表·聚合）+ funcs 门面（20 行）；依赖严格单向 （convert ← 各类别 ← registry），`funcs.FUNCS` 仍 160、`all_names()` 仍 175；新增 symtable 全局扫描，抓出拆分时遗漏的 `_date`→`_EPOCH` 跨模块引用并修复；`warehouse.spec` 清单重生成（92 条、0 缺失） |
| v3.92 | **导入与桌面外壳去臃肿**：`wh/importer.py`(892) 拆为 `importer/`（base / wide / txn 三层，依赖单向）；`wh/desktop.py` + `wh/qtwin.py` 合为 `desktop/`（shell + qt）；`sheet/web/routes.py`(886) 拆为 common / pages / io_rt / api 四个文件。顺带解开两处反向依赖：`_read_grid` 由 base 下移到 txn、撤销栈由路由层下沉到 ops，`warehouse.spec` 模块清单改为按实际结构自动生成（83 条、0 缺失） |
| v3.91 | **采购模块去臃肿**：`logic.py`(965) + `routes.py`(894) 拆为 amount / head / summary / status / receive / query 六个逻辑文件 + `web/`（order·tpl·supplier·export）四个路由文件；`logic.py` 整个删掉，6 处外部调用改为直连子模块；采购包不再 import `table`/`importer` |
| v3.90 | **核心去臃肿**：`wh/core/db.py`（1545 行）拆为 paths / dbconn / schema / query / tpl / potpl / maintain 七个子模块 + 18 行门面；门面公开符号 **83 个与拆前完全一致**，24 个调用方零改动；`_cols` 下沉解开了 schema↔potpl 循环依赖；`warehouse.spec` 模块清单改为按实际结构自动生成（67 条，0 缺失） |
| v3.89 | **业务模块去臃肿**：`wh/stat/` 拆为 report / center，`wh/txn/` 拆为 form / importer / browse；修正 `warehouse.spec` 中 39 条指向已搬走模块的过时 hiddenimports |
| v3.99 | **修复 APK 构建中断**：`build-apk.yml` 的「同步版本号」步骤里嵌套引号写法（`python3 -c "...r\"__version__...['\\"]...` ）无法被 shell 解析，报 `syntax error near unexpected token '('`。**关键是语法错误在解析阶段就杀掉整段脚本**，即便该分支（手动运行兜底）本次不会执行也照样失败。改为无嵌套引号的 `grep -m1 '__version__' wh/__init__.py | tr -cd '0-9.'`。实测 `bash -n` / `sh -n` 均通过；tag v3.98→versionName 3.98/versionCode 398，手动运行回退读 `__version__`→3.99/399 两种场景均验证正确 |
| v3.98 | **制表静态资源兜底路径修正**：`univer.static_dir()` 在无请求上下文时的兜底分支少上溯一层（指向 `wh/static` 而非项目根 `static`），会把新引擎资源误判为「未随包安装」从而静默回退旧制表台；改为上溯 4 层。引擎链路复核：偏好 new/old 下新建分别跳 `/sheet/<id>/univer` 与 `/sheet/<id>`、列表主链接与提示同步、新簿默认 1000 行×200 列、写入第 301 列后 Univer 快照自动扩至 302 列且数据可见 |
| v3.97 | **制表默认引擎与网格尺寸修正**：`/sheet/new` 原忽略引擎偏好、一律跳旧制表台（实测 302→`/sheet/<id>`），改为偏好新引擎且资源随包装上时跳 `/sheet/<id>/univer`；新建工作表默认网格 200×26 提至 **1000 行 × 200 列**（`kernel.addr.DEFAULT_ROWS/COLS`，Univer 快照下限同步）；旧制表台滚动扩展上限 行 4000→20000、列 200→2000，初始视窗 30×12→60×20。 |
| v3.96 | **CSV 响应头规范化**：5 处 `mimetype='text/csv; charset=utf-8'` 改为 `content_type=`，消除 Werkzeug 追加导致的 `charset=utf-8; charset=utf-8` 重复；12 个 CSV 导出实测 Content-Type 均只含 1 个 charset、BOM 与中文完好 |
